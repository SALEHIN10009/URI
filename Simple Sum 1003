#include<iostream>
using namespace std;

int main(){
    int A, B;
    cin >> A >> B ;                        //scanf("%d%d", &A,&B);
    cout << "SOMA = " << A+ B << endl;    // printf("SOMA = %d\n", A+B);
    return 0;
}
